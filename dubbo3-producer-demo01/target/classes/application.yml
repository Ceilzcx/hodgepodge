spring:
  application:
    name: dubbo_v3-producer01

dubbo:
  application:
    name: ${spring.application.name}
  registry:
    address: zookeeper://127.0.0.1:2181
  protocol:
    port: 20880
    name: dubbo

server:
  port: 8081